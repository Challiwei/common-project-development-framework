---
任务编号: T-{编号}
创建时间: 
负责人: 
所属阶段: 
母任务编号: (如果是子任务，填写此处)
状态: 待评估 / 进行中 / 已拆分 / 已完成
独立对话链接: 
---

# 任务：{任务名称}

## 1. 任务背景信息
> 执行该任务需要了解的上下文。必须明确指定并提供阅读链接。

- **源需求文档**：[[03-产品需求文档/{对应PRD文件}]]
- **前置输出物**：(如技术初步共识纪要、上一阶段的架构图等)
- **其他关键背景**：

## 2. 规范与约束条件
> 执行过程中必须遵守的硬性约束。

- **全局规范**：(如：[[00-流程与规范/版本管理规范]])
- **项目级约束**：(如前端必须使用 React，后端必须使用 Go 等)
- **协作级约束**：(必须保证与某某文档中的约定一致)

## 3. 验收标准 (DoD - Definition of Done)
> 什么样的输出算作任务彻底完成？

1. 
2. 
3. 

## 4. 上下文容量评估 (Context Size Check)
> ⚠️ **所有 Agent 必须在执行任务前进行此项评估！**

- [ ] **预判问题**：当前任务的"输入（背景文档） + 执行推理过程 + 预期的输出内容"，预估总长度是否超过当前对话大模型上下文长度上限的 **65%**？
  - **评估结果为【否，< 65%】**：该任务可以直接在这个独立的对话中执行。
  - **评估结果为【是，> 65%】**：**立刻停止执行！**将本任务状态改为"已拆分"，并在下方"子任务拆分"区域将其拆分为多个子任务（A、B...）。

## 5. 容量超限拆分 (按需使用：处理大任务)
> 当母任务自身工作量超过 65% 容量时使用。拆分出的每个子任务也必须使用本卡片模板，并各自评估容量。

- **子任务 A**：[[T-xxx-1-子任务名称]]（必须在全新的对话中独立执行）
- **子任务 B**：[[T-xxx-2-子任务名称]]（必须在全新的对话中独立执行）

## 6. 跨界依赖协同 (按需使用：处理外部依赖)
> ⚠️ **在执行过程中，如果发现当前任务受阻，必须依赖其他 Agent（其他板块）配合时触发！**

- [ ] **1. 发现依赖**：明确说明你需要谁、为你提供什么。（例：我需要【后端架构师】为我提供【获取用户列表的接口】）
- [ ] **2. 派发协同任务**：创建一个新的任务卡片分配给对方，例如：[[T-xxx-协同-获取用户接口设计]]
- [ ] **3. 双边总监会签**：【发起方架构师/总监】与【接收方架构师/总监】必须在新对话中进行一次**"微型初步共识会议"**。双方对该依赖的契约（例如 API 的字段规范）达成一致，并写入该协同任务的「约束条件」中。
- [ ] **4. 挂起等待**：本任务暂时挂起，等待协同任务完成后，再继续执行。

## 7. 执行与产出物
> 在此处附上最终成功生成的文档链接。

- **产出物链接**：[[最终产出文档路径]]
